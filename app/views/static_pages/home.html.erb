<div class="center">
  <h1>Welcome to the Device Treasury</h1>
</div>

<% if logged_in? %>
  <h2>
    <%= button_to "Checkout a Device", devices_url, method: :get, class: "btn btn-success btn-lg" %>
  </h2>

  <% if !@checkouts.empty? %>
    <h3>Checkout History</h3>
    <div class="pagination">
      <%= will_paginate @checkouts %>
    </div>
    <%= render 'shared/checkout_history' %>
    <div class="pagination">
      <%= will_paginate @checkouts %>
    </div>
  <% end %>

<% end %>
